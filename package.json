{
  "name": "next-calendar-events-server",
  "version": "1.0.0",
  "description": "Fetch next events in multiples calendars and write them in a json file",
  "main": "src/index.js",
  "scripts": {
    "start": "mkdir -p ./dist && node src/index.js",
    "tdd": "jest --watch",
    "test": "jest"
  },
  "author": "Grégory PAUL",
  "license": "GPL-3.0-only",
  "dependencies": {
    "node-ical": "^0.13.0",
    "rrule": "^2.7.1"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  }
}
