{
  "name": "next-calendar-events-server",
  "version": "1.0.0",
  "description": "Fetch next events in multiples calendars and write them in a json file",
  "main": "src/index.js",
  "scripts": {
    "debug": "DEBUG=cal node src/index.js",
    "deps": "mkdir -p ./dist",
    "start": "node src/index.js",
    "test": "vitest"
  },
  "author": "Grégory PAUL",
  "license": "GPL-3.0-only",
  "dependencies": {
    "debug": "^4.4.3",
    "node-ical": "^0.26.0",
    "rrule": "^2.8.1"
  },
  "devDependencies": {
    "vitest": "^4.1.6"
  }
}
